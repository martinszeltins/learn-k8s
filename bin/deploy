#!/bin/sh
set -e

# Placeholder: deploy to K3s cluster
# Prerequisites: kubectl configured and pointing to your K3s cluster

cd "$(dirname "$0")/.."

echo "Applying Kubernetes manifests..."
kubectl apply -f docker/k8s/

echo "Done! Check status with: kubectl get pods"
